<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <link rel="stylesheet" href="../../public/styles/pages/login-page.css" />
  </head>
  <body id="sign-in-page">
    <header><%-include("../partials/navbar") %></header>
    <main data-stack data-center>
      <h1 class="title">WELCOME BACK!</h1>
      <form id="login-form" action="/auth/sign-in" method="post" data-stack>
        <div data-stack>
          <label for="username">Username</label>
          <input type="text" name="username" id="username"
          value="<%=formData["username"] %>"/>
          <span class="error-msg">
            <% errors.forEach(err => {if (err.path==="username") {%> <%= err.msg
            %> <% }}) %>
          </span>
        </div>

        <div data-stack>
          <label for="password">Password</label>
          <input type="password" name="password" id="password"
          value="<%=formData["password"] %>"/>
          <span class="error-msg">
            <% errors.forEach(err => {if (err.path==="password") {%> <%= err.msg
            %> <% }}) %>
          </span>
        </div>
        <button type="submit">Sign In</button>
      </form>
    </main>

    <footer><%-include("../partials/footer") %></footer>
  </body>
</html>
